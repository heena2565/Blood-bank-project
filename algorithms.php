<?php
//Linear search 
/*function getInput(){
    return trim(fgets(STDIN));
}
Function linearSearch($arr, $target){
    $n = count($arr);
    for($i=0; $i < $n; $i++){
       if( $arr[$i]== $target)
       {
        return $i;
       }
    }
    return -1;
}
echo "Enter the elements of array(int): separated by space:";
$intElement = getInput();
$number = explode(' ', $intElement);


$target = readline("enter the value of target element");
$result= linearSearch($number, $target);
 if($result != -1){
    echo "Element found on index : $result";
 }
 else{
    echo "Element not found";
 }*/
 //brute force algorithm
 

?> 